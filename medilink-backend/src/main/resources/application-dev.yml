spring:
  jpa:
    show-sql: true
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/medilink_dev}

logging:
  level:
    root: INFO
    com.medilink: DEBUG
    org.springframework.security: DEBUG
